# Normalize line endings to LF.
*                       text eol=lf

# Unix shell scripts must have LF.
*.{sh,[sS][hH]}         text eol=lf
gradlew                 text eol=lf
mvnw                    text eol=lf

# Windows batch files must have CRLF.
*.{bat,[bB][aA][tT]}    text eol=crlf
*.{cmd,[cC][mM][dD]}    text eol=crlf

# Ensure that line endings for multipart files in spring-web are not modified.
*.multipart             -text

# binary is a macro for '-text -diff'
*.gif                   binary
*.jar                   binary
*.jks                   binary
*.jpeg                  binary
*.jpg                   binary
*.p12                   binary
*.png                   binary
*.svg                   text
*.zip                   binary
#*.zip filter=lfs diff=lfs merge=lfs -text

*.aif                   binary
*.au                    binary
*.flac                  binary
*.m4a                   binary
*.m4b                   binary
*.mp3                   binary
*.pgg                   binary
*.wav                   binary
*.wma                   binary
